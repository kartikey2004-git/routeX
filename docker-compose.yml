# YAML: Yet Another Markup Language
# docker compose file for database

name: routex
services:
  db:
    image: postgres:latest
    environment:
      - POSTGRES_USER=Kartikey
      - POSTGRES_PASSWORD=npg_E2sQMClHxT1p
      - POSTGRES_DB=routex
    ports:
      - 5432:5432
